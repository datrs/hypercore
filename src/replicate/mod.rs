// use sparse_bitfield::Bitfield;

/// A peer on the network.
// omitted fields: [
//  feed,
//  stream,
//  inflightRequests,
// ]
pub struct Peer {
  // remote_id: usize,
// remote_length: usize,
// remote_bitfield: Bitfield,
// remote_is_want: bool,
// remote_is_downloading: bool,
// is_live: bool,
// is_sparse: bool,
// is_downloading: bool,
// is_uploading: bool,
// max_requests: u16,
}
