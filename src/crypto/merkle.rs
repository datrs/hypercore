// extern crate merkle_tree_stream;
// use self::merkle_tree_stream::MerkleTreeStream;

/// Merkle Tree Stream
#[derive(Debug)]
pub struct Merkle {}

impl Merkle {
  /// Create a new instance.
  pub fn new() -> Self {
    unimplemented!();
  }

  /// Access the next item.
  pub fn next(&self, _data: &[u8]) {
    unimplemented!();
  }
}
