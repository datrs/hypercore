/// A `BLAKE2b` hash.
pub struct Hash {}

impl Hash {
  /// Hash a `Leaf` node.
  pub fn from_leaf() -> Self {
    unimplemented!();
  }

  /// Hash a `Parent` node.
  pub fn from_parent() -> Self {
    unimplemented!();
  }

  /// Hash a vector of `Root` nodes.
  pub fn from_roots() -> Self {
    unimplemented!();
  }
}
